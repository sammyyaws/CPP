📌 Requirements

Before running this program, ensure you have the following installed on your system:

   ---- C++ Compiler – GCC (Linux/macOS) or MinGW/MSVC (Windows)

   ---- CMake Build Tool – Download from https://cmake.org/download/ if not already installed.


1:Make Sure Cmake build tool is installed
2:Open the directory of the program in your terminal
cd yourpath/to/project
3.Run the folowing

......................................>>>>>>>>>.......................................
mkdir build
cd build
cmake ..
make

4.This step generate an executable that you can run
...............................on Linux .....................................
execute ./LIBMAS in the build directory to run
...............................on Windows.....................................
execute ./LIBMAS.exe in the build directory to run